import { Story, Meta } from '@storybook/react'
import FormBanner from '.'

export default {
  title: 'Form/FormBanner',
  component: FormBanner
} as Meta

export const Default: Story = () => <FormBanner />
