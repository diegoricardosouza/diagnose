import { Story, Meta } from '@storybook/react'
import FormAgenda from '.'

export default {
  title: 'Form/FormAgenda',
  component: FormAgenda
} as Meta

export const Default: Story = () => <FormAgenda />
